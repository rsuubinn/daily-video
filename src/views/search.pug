extends layouts
include mixins/video

block content
  form(method="GET")
    input(type="text", placeholder="검색어를 입력하세요.", name="keyword", required)
    input(type="submit", value="검색")
  div
    each video in videos
      +videoMixin(video)
